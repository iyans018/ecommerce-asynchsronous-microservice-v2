{
  "name": "shipping-service",
  "version": "1.0.0",
  "main": "index.js",
  "author": "oktavian aji",
  "license": "MIT",
  "scripts": {
    "start": "NODE_ENV=prod nodemon --es-module-specifier-resolution=node src/index.js",
    "dev": "NODE_ENV=dev nodemon --es-module-specifier-resolution=node src/index.js",
    "testing": "NODE_ENV=test nodemon --es-module-specifier-resolution=node src/index.js",
    "test": "NODE_ENV=test mocha --es-module-specifier-resolution=node --timeout 10000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "joi": "^17.6.0",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.3.5",
    "redis": "3.1.2"
  },
  "type": "module",
  "devDependencies": {
    "chai": "^4.3.6",
    "chai-http": "^4.3.0",
    "mocha": "^10.0.0"
  }
}
